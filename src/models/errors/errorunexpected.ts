/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import * as models from "../index.js";
import { DailyPayError } from "./dailypayerror.js";

/**
 * Unexpected error occured
 */
export type ErrorUnexpectedData = {
  /**
   * A list of errors that occurred.
   */
  errors: Array<models.ErrorUnexpectedError>;
  httpMeta: models.HTTPMetadata;
};

/**
 * Unexpected error occured
 */
export class ErrorUnexpected extends DailyPayError {
  /**
   * A list of errors that occurred.
   */
  errors: Array<models.ErrorUnexpectedError>;

  /** The original data that was passed to this error instance. */
  data$: ErrorUnexpectedData;

  constructor(
    err: ErrorUnexpectedData,
    httpMeta: { response: Response; request: Request; body: string },
  ) {
    const message = "message" in err && typeof err.message === "string"
      ? err.message
      : `API error occurred: ${JSON.stringify(err)}`;
    super(message, httpMeta);
    this.data$ = err;
    this.errors = err.errors;

    this.name = "ErrorUnexpected";
  }
}

/** @internal */
export const ErrorUnexpected$inboundSchema: z.ZodType<
  ErrorUnexpected,
  z.ZodTypeDef,
  unknown
> = z.object({
  errors: z.array(models.ErrorUnexpectedError$inboundSchema),
  HttpMeta: models.HTTPMetadata$inboundSchema,
  request$: z.instanceof(Request),
  response$: z.instanceof(Response),
  body$: z.string(),
})
  .transform((v) => {
    const remapped = remap$(v, {
      "HttpMeta": "httpMeta",
    });

    return new ErrorUnexpected(remapped, {
      request: v.request$,
      response: v.response$,
      body: v.body$,
    });
  });
