/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * A list of links to resources that may be helpful in resolving the error.
 */
export type ErrorConflictErrorLinks = {
  about?: string | undefined;
};

/**
 * Location in the request that may have caused the error.
 */
export type ErrorConflictErrorSource = {
  /**
   * The name of the parameter that caused the error.
   */
  parameter?: string | undefined;
  /**
   * A JSON Pointer to the location in the request that caused the error.
   */
  pointer?: string | undefined;
  /**
   * The name of the header that caused the error.
   */
  header?: string | undefined;
};

/**
 * Additional information about the error.
 */
export type ErrorConflictErrorMeta = {
  /**
   * A UUID for the originating request.
   */
  requestId?: string | undefined;
  /**
   * An ID used for tracing purposes.
   */
  traceId?: string | undefined;
};

export type ErrorConflictError = {
  /**
   * A code that indicates what went wrong.
   */
  code: string;
  /**
   * The HTTP status code for the error.
   */
  status: string;
  /**
   * A message that explains the meaning of the error code. Developers are advised not to make programmatic use of this value, as it may change
   */
  detail: string;
  /**
   * A list of links to resources that may be helpful in resolving the error.
   */
  links: ErrorConflictErrorLinks;
  /**
   * Location in the request that may have caused the error.
   */
  source?: ErrorConflictErrorSource | undefined;
  /**
   * Additional information about the error.
   */
  meta: ErrorConflictErrorMeta;
};

/** @internal */
export const ErrorConflictErrorLinks$inboundSchema: z.ZodType<
  ErrorConflictErrorLinks,
  z.ZodTypeDef,
  unknown
> = z.object({
  about: z.string().optional(),
});

export function errorConflictErrorLinksFromJSON(
  jsonString: string,
): SafeParseResult<ErrorConflictErrorLinks, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ErrorConflictErrorLinks$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ErrorConflictErrorLinks' from JSON`,
  );
}

/** @internal */
export const ErrorConflictErrorSource$inboundSchema: z.ZodType<
  ErrorConflictErrorSource,
  z.ZodTypeDef,
  unknown
> = z.object({
  parameter: z.string().optional(),
  pointer: z.string().optional(),
  header: z.string().optional(),
});

export function errorConflictErrorSourceFromJSON(
  jsonString: string,
): SafeParseResult<ErrorConflictErrorSource, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ErrorConflictErrorSource$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ErrorConflictErrorSource' from JSON`,
  );
}

/** @internal */
export const ErrorConflictErrorMeta$inboundSchema: z.ZodType<
  ErrorConflictErrorMeta,
  z.ZodTypeDef,
  unknown
> = z.object({
  request_id: z.string().optional(),
  trace_id: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "request_id": "requestId",
    "trace_id": "traceId",
  });
});

export function errorConflictErrorMetaFromJSON(
  jsonString: string,
): SafeParseResult<ErrorConflictErrorMeta, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ErrorConflictErrorMeta$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ErrorConflictErrorMeta' from JSON`,
  );
}

/** @internal */
export const ErrorConflictError$inboundSchema: z.ZodType<
  ErrorConflictError,
  z.ZodTypeDef,
  unknown
> = z.object({
  code: z.string(),
  status: z.string(),
  detail: z.string(),
  links: z.lazy(() => ErrorConflictErrorLinks$inboundSchema),
  source: z.lazy(() => ErrorConflictErrorSource$inboundSchema).optional(),
  meta: z.lazy(() => ErrorConflictErrorMeta$inboundSchema),
});

export function errorConflictErrorFromJSON(
  jsonString: string,
): SafeParseResult<ErrorConflictError, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ErrorConflictError$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ErrorConflictError' from JSON`,
  );
}
